name: token_counter_example
description: "Example Flutter app for the token_counter package."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  token_counter:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
